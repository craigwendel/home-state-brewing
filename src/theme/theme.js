import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#244f70',
    },
    secondary: {
      main: '#FBAE21',
    },
    background: {
      default: '#fff',
      paper: '#fafafa',
    },
  },
});

export default theme;
